export default class HomeScreen {
  #containerDiv;

  constructor(_gameBoard) {
    this.#containerDiv = _gameBoard;
  }

  showHomeScreen() {
    // This will just make the home screen appear and function.
  }

  hideHomeScreen() {
    // This will stop the home screen from functioning, and make it disappear.
  }
}
